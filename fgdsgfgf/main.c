#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define CANT 3
int main()
{
    int legajo[CANT];
    char nombre[CANT][31];//cant--cant de nombres(columna) 31-- cant de elemento del nombr(fila)//

    for(int i=0; i<CANT; i++)
    {

        printf("legajo");
        legajo[i]=i+1;
        printf("nombre");//----->&nombre[i[0]
        fflush(stdin);
        scanf("%[^\n]",nombre[i]);

    }
    for()
    return 0;
}
